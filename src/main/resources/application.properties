spring.application.name=pagepulse

server.port=8080

# CORS is configured in AuditController via @CrossOrigin
# Timeout settings used by AuditService (in milliseconds)
pagepulse.request.timeout=8000

logging.level.root=INFO
logging.level.com.digitalheroes.pagepulse=INFO
